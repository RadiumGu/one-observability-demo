# PetSite 线上环境部署审阅报告

> 审阅日期：2026-03-28
> 审阅范围：EKS 集群 `PetSite`（ap-northeast-1）上 petsite 及其相关应用
> 审阅者：架构审阅猫（AWS PSA + 客户 PA 双视角）

---

## 一、线上资源清单

| 服务 | Deployment | Replicas | 镜像来源 | Sidecar | ServiceAccount |
|------|-----------|----------|----------|---------|----------------|
| PetSite 前端 | petsite-deployment | 2 | petadoptions/petsite:latest | 无（X-Ray DaemonSet） | petsite-sa (IRSA) |
| PayForAdoption | pay-for-adoption | 2 | CDK asset hash 镜像 | aws-otel-collector v0.41.1 | pay-for-adoption-sa (IRSA) |
| ListAdoptions | list-adoptions | 2 | CDK asset hash 镜像 | aws-otel-collector v0.41.1 | list-adoptions-sa (IRSA) |
| SearchService | search-service | 2 | CDK asset hash 镜像 | aws-otel-collector v0.41.1 | search-service-sa (IRSA) |
| PetHistory | pethistory-deployment | 2 | pet-adoptions-history:latest | aws-otel-collector latest | pethistory-sa (IRSA) |
| TrafficGenerator | traffic-generator | 1 | CDK asset hash 镜像 | 无 | — |
| X-Ray Daemon | xray-daemon (DaemonSet) | 3 | — | — | — |

**基础设施：**
- EKS 节点组：3 x t4g.xlarge (arm64, ON_DEMAND)，min 3 / max 6
- ALB：`Servic-PetSi-by0kpyBtxswj`（internet-facing），HTTP:80 + HTTPS:443
- 数据库：Aurora PostgreSQL Serverless（单 AZ）、Neptune db.r6g.large（单 AZ）
- 服务间调用：Kubernetes 内部 DNS（通过 SSM Parameter Store `/petstore/*` 配置）
- 认证：Cognito（仅 HTTPS:443 listener 上启用）

---

## 二、问题与修改建议

### 🔴 P0 — 必须立即修复

#### 2.1 HTTP 80 未 redirect 到 HTTPS（认证绕过）

**问题：** ALB HTTP:80 listener 的默认 action 是 `forward`，直接把流量转发到 petsite。HTTPS:443 上配了 Cognito 认证，但 HTTP:80 完全没有。用户可以通过 `http://` 绕过认证直接访问应用。

**修复方式：**

```bash
# 获取 HTTP:80 listener ARN
ALB_ARN="arn:aws:elasticloadbalancing:ap-northeast-1:926093770964:loadbalancer/app/Servic-PetSi-by0kpyBtxswj/bbe5082588a126fc"
LISTENER_80=$(aws elbv2 describe-listeners --load-balancer-arn "$ALB_ARN" \
  --query 'Listeners[?Port==`80`].ListenerArn' --output text)

# 修改为 redirect → HTTPS
aws elbv2 modify-listener --listener-arn "$LISTENER_80" \
  --default-actions '[{
    "Type": "redirect",
    "RedirectConfig": {
      "Protocol": "HTTPS",
      "Port": "443",
      "StatusCode": "HTTP_301"
    }
  }]'
```

**验证：**
```bash
curl -v http://<ALB_DNS>/ 2>&1 | grep "301\|Location"
# 应该看到 301 Moved Permanently → https://...
```

---

#### 2.2 Aurora PostgreSQL 未开启存储加密

**问题：** 集群 `serviceseks2-databaseb269d8bb-efjeyzicx2ak` 的 `StorageEncrypted: False`。存着 adoption 业务数据，合规审计直接不通过。Neptune 和 Grafana Aurora 都加密了，唯独主业务库没加密。

**注意：** Aurora 加密只能在创建时开启，事后无法直接修改。需要通过 snapshot → restore 方式迁移。

**修复步骤：**

```bash
DB_CLUSTER="serviceseks2-databaseb269d8bb-efjeyzicx2ak"

# 1. 创建快照
aws rds create-db-cluster-snapshot \
  --db-cluster-identifier "$DB_CLUSTER" \
  --db-cluster-snapshot-identifier "${DB_CLUSTER}-pre-encryption-snapshot"

# 2. 等待快照完成
aws rds wait db-cluster-snapshot-available \
  --db-cluster-snapshot-identifier "${DB_CLUSTER}-pre-encryption-snapshot"

# 3. 从快照恢复为加密集群（使用 AWS 默认 KMS key 或指定 key）
aws rds restore-db-cluster-from-snapshot \
  --db-cluster-identifier "${DB_CLUSTER}-encrypted" \
  --snapshot-identifier "${DB_CLUSTER}-pre-encryption-snapshot" \
  --engine aurora-postgresql \
  --storage-encrypted \
  --kms-key-id alias/aws/rds \
  --serverless-v2-scaling-configuration MinCapacity=0.5,MaxCapacity=4

# 4. 创建实例
aws rds create-db-instance \
  --db-instance-identifier "${DB_CLUSTER}-encrypted-instance" \
  --db-cluster-identifier "${DB_CLUSTER}-encrypted" \
  --db-instance-class db.serverless \
  --engine aurora-postgresql

# 5. 更新 SSM Parameter Store 里的 endpoint
# 6. 重启依赖服务
# 7. 验证正常后删除旧集群
```

⚠️ 需要安排维护窗口，涉及短暂停机（切换 endpoint 期间）。

---

#### 2.3 Aurora PostgreSQL 未开启删除保护

**问题：** `DeletionProtection: False`。误操作 `cdk destroy` 或 CloudFormation stack 删除会直接丢失数据库。

**修复：**

```bash
aws rds modify-db-cluster \
  --db-cluster-identifier "serviceseks2-databaseb269d8bb-efjeyzicx2ak" \
  --deletion-protection \
  --apply-immediately
```

同时建议增加备份保留天数（当前仅 1 天）：

```bash
aws rds modify-db-cluster \
  --db-cluster-identifier "serviceseks2-databaseb269d8bb-efjeyzicx2ak" \
  --backup-retention-period 7 \
  --apply-immediately
```

---

### 🟡 P1 — 短期优化（1-2 周内）

#### 2.4 pethistory liveness probe 过于激进

**问题：** `initialDelaySeconds: 3, periodSeconds: 3`，Python 应用 3 秒就开始探活、每 3 秒一次。其他服务都是 30s/10s，这个异常激进，容易导致启动慢时被误杀进入 CrashLoopBackOff。

**修复：**

```bash
kubectl patch deploy pethistory-deployment -p '{
  "spec": {
    "template": {
      "spec": {
        "containers": [{
          "name": "pethistory",
          "livenessProbe": {
            "initialDelaySeconds": 30,
            "periodSeconds": 10,
            "failureThreshold": 3
          }
        }]
      }
    }
  }
}'
```

---

#### 2.5 pethistory OTEL sidecar 使用 `latest` tag

**问题：** pethistory 用 `amazon/aws-otel-collector:latest`，其他三个服务用 `public.ecr.aws/aws-observability/aws-otel-collector:v0.41.1`。latest 可能在某次拉取时突然不兼容。

**修复：**

```bash
kubectl set image deploy/pethistory-deployment \
  aws-otel-collector=public.ecr.aws/aws-observability/aws-otel-collector:v0.41.1
```

---

#### 2.6 数据库全部单 AZ

**问题：** Aurora PostgreSQL 和 Neptune 都是单实例、单 AZ。EKS 节点分布在 2 个 AZ，但数据层是单点故障。

**修复建议（Aurora）：**

```bash
# 添加 reader 实例到另一个 AZ
aws rds create-db-instance \
  --db-instance-identifier "serviceseks2-db-reader" \
  --db-cluster-identifier "serviceseks2-databaseb269d8bb-efjeyzicx2ak" \
  --db-instance-class db.serverless \
  --engine aurora-postgresql \
  --availability-zone ap-northeast-1c  # 和 writer 不同的 AZ
```

**修复建议（Neptune）：**

```bash
aws neptune create-db-instance \
  --db-instance-identifier "petsite-neptune-instance-2" \
  --db-cluster-identifier "petsite-neptune" \
  --db-instance-class db.r6g.large \
  --engine neptune \
  --availability-zone ap-northeast-1c  # 和 instance-1 不同的 AZ
```

> 注意：Neptune reader 实例会增加约 $0.312/hr（db.r6g.large），月增 ~$225。评估 demo 是否需要这个投入。

---

### 🟢 P2 — 中期改进

#### 2.7 后端服务资源过度配置

**问题：** pay-for-adoption、list-adoptions、search-service 的 requests = limits = 512m CPU / 1Gi 内存（Guaranteed QoS）。实际 CPU 使用率 1-4%，纯粹浪费。

**建议调整：**

```yaml
# 对每个后端服务
resources:
  requests:
    cpu: "128m"
    memory: "512Mi"
  limits:
    cpu: "512m"
    memory: "1Gi"
```

示例命令：
```bash
for deploy in pay-for-adoption list-adoptions search-service; do
  kubectl patch deploy "$deploy" -p '{
    "spec": {
      "template": {
        "spec": {
          "containers": [{
            "name": "'$deploy'",
            "resources": {
              "requests": {"cpu": "128m", "memory": "512Mi"},
              "limits": {"cpu": "512m", "memory": "1Gi"}
            }
          }]
        }
      }
    }
  }'
done
```

---

#### 2.8 添加 HPA

**问题：** 所有服务固定 2 replicas，没有自动伸缩。

**建议：** 至少为 petsite 和 search-service 添加 HPA。

```yaml
# hpa-petsite.yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: petsite-hpa
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: petsite-deployment
  minReplicas: 2
  maxReplicas: 6
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 60
---
# hpa-search.yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: search-service-hpa
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: search-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 60
```

---

#### 2.9 添加 PDB

**问题：** 没有 PodDisruptionBudget，节点维护时可能所有 Pod 同时被驱逐。

```yaml
# pdb-petsite.yaml
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: petsite-pdb
  namespace: default
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: petsite
---
# 对每个后端服务类似
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: pay-for-adoption-pdb
  namespace: default
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: pay-for-adoption
```

---

#### 2.10 节点成本优化

**问题：** 3 x t4g.xlarge ON_DEMAND，CPU 利用率 1-4%，明显 over-provisioned。

**建议：**
- demo 环境考虑 Spot instances（可节省 60-70%）
- 或降级为 t4g.large（4 GiB → 8 GiB 内存，CPU 2 cores），按当前负载完全够用
- 预估月度节省：从 ~$290 降至 ~$145（t4g.large）或 ~$90（Spot t4g.xlarge）

---

#### 2.11 Namespace 隔离（可选）

**问题：** 所有服务在 `default` namespace，无法做细粒度 RBAC / NetworkPolicy / ResourceQuota。

如果后续需要作为参考架构给客户演示，建议迁移到 `petadoptions` namespace（和 repo 一致）。迁移步骤：
1. 创建 namespace
2. 复制 ServiceAccount（保留 IRSA annotation）
3. 更新 SSM Parameter Store 里的 `.default.svc.cluster.local` → `.petadoptions.svc.cluster.local`
4. 重新部署所有服务
5. 更新 TargetGroupBinding

---

## 三、优先级总览

| 优先级 | 问题 | 影响 | 工作量 |
|--------|------|------|--------|
| 🔴 P0 | HTTP 80 认证绕过 | 安全漏洞 | 5 分钟 |
| 🔴 P0 | Aurora 删除保护 + 备份保留 | 数据安全 | 5 分钟 |
| 🔴 P0 | Aurora 存储加密 | 合规要求 | 2-3 小时（需维护窗口） |
| 🟡 P1 | pethistory liveness probe | 稳定性 | 5 分钟 |
| 🟡 P1 | OTEL collector tag 统一 | 稳定性 | 5 分钟 |
| 🟡 P1 | 数据库 Multi-AZ | 可用性 | 30 分钟 + 成本评估 |
| 🟢 P2 | 后端资源调整 | 成本优化 | 15 分钟 |
| 🟢 P2 | 添加 HPA | 弹性 | 15 分钟 |
| 🟢 P2 | 添加 PDB | 稳定性 | 10 分钟 |
| 🟢 P2 | 节点成本优化 | 成本优化 | 30 分钟 |
| 🟢 P2 | Namespace 隔离 | 架构规范 | 1-2 小时 |
